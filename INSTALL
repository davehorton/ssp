From the top-level (ssp) directory:

-autoreconf -fvi

then

mkdir debug release
cd release 
../configure CPPFLAGS="-DNDEBUG -DVERSION=\\"\"$(git describe)\\"\""
make
make install

cd ../debug
../configure CPPFLAGS="-NDEBUG -DVERSION=\\"\"$(git describe)\\"\"" CXXFLAGS='-g -O0'
make 